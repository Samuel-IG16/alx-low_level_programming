/**
 * set_string - sets value of a pointer to a char
 * @s: double pointer to a string
 * @to: the pointer to the string to set s to
 *
 * Return: void
 */
void set_string(char **s, char *to)
{
	*s = to;
}
